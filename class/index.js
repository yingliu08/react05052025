const { add } = require("./util/math");

console.log(add(2, 5));

const path = require("path");
const os = require("os");
const fs = require("fs");
const { v4: uuidv4 } = require("uuid");
console.log(uuidv4());
